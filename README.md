# Gradwork Backend
written in rust so we could handle users from kepler

# Setup

## Add this shit in enviroments file
```
DATABASE_URL=
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_JWT_SECRET=

```
run migrations
``cargo run -- migrate``

``cargo run``
